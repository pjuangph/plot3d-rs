[package]
name = "plot3d"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "plot3d"
path = "src/lib.rs"

[dependencies]
byteorder = "1.5"
thiserror = "1.0"

# for tests (download)
reqwest = { version = "0.12", features = ["blocking"] }

[dev-dependencies]
tempfile = "3.10"
